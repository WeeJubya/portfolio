/**
 * File: Header.tsx
 * Purpose: Top navigation / header component for the portfolio site.
 */

import React from 'react'
import { Mail, Menu } from 'lucide-react'

/**
 * HeaderProps interface
 * @interface
 * @property onContactClick - callback when contact button is clicked
 */
export interface HeaderProps {
  onContactClick?: () => void
}

/**
 * Header component
 * Renders site title and a simple action to contact the author.
 */
export const Header: React.FC<HeaderProps> = ({ onContactClick }) => {
  return (
    <header className="w-full py-4 px-6 flex items-center justify-between bg-gradient-to-r from-slate-900 to-indigo-900 text-white">
      <div className="flex items-center gap-3">
        <div className="rounded-md bg-teal-400/10 p-2">
          <Menu className="text-teal-300" />
        </div>
        <div>
          <h1 className="text-lg font-semibold">Glynn Williams</h1>
          <p className="text-xs text-teal-200/80">eLearning Portfolio</p>
        </div>
      </div>

      <button
        onClick={onContactClick}
        className="inline-flex items-center gap-2 rounded-md bg-white/10 px-3 py-2 text-sm hover:bg-white/20"
        aria-label="Contact"
      >
        <Mail size={16} />
        Contact
      </button>
    </header>
  )
}

export default Header
